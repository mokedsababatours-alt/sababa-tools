# ─── COPY THIS FILE TO .env AND FILL IN YOUR VALUES ───────────────────────────
# Never commit .env to Git. This file (.env.example) is safe to commit.

# ── Database ────────────────────────────────────────────────────────────────────
# Path to the SQLite file. In production this lives in the volume you mount.
DATABASE_URL="file:./prisma/portal.db"

# ── Auth ─────────────────────────────────────────────────────────────────────────
# Your portal domain (team-facing)
NEXTAUTH_URL="https://tools.yourdomain.com"

# Generate with: openssl rand -base64 32
# Or just use any long random string
AUTH_SECRET="REPLACE_WITH_LONG_RANDOM_STRING"


# ── n8n Chat Webhook URLs ─────────────────────────────────────────────────────────
# Paste your n8n webhook URL for each AI tool you create
# You can add more with any name following this pattern
N8N_WEBHOOK_MARKETING="https://your-n8n.yourdomain.com/webhook/marketing-assistant"
N8N_WEBHOOK_CUSTOMER="https://your-n8n.yourdomain.com/webhook/customer-reply"

# ── Admin User ────────────────────────────────────────────────────────────────────
# Used only once during first-time setup (npm run db:seed)
ADMIN_EMAIL="you@yourdomain.com"
ADMIN_PASSWORD="change-this-strong-password"
ADMIN_NAME="מנהל"
